services:
  - type: web
    name: my-app
    env: node
    plan: free
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production
      - key: API_KEY
        value: I7WjClkzXc4
    autoDeploy: true
    repo: https://github.com/skoegletestproject/production-Server.git

jobs:
  deploy-job:
    runs-on: ubuntu-latest
    on:
      push:
        branches:
          - main
    steps:
      - name: Deploy to Render
        run: curl -X POST "https://api.render.com/deploy/srv-culhm9d2ng1s73asc690?key=I7WjClkzXc4"
